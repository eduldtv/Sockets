<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="es.fempa.juanpomares.sockets">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
<!--         android:roundIcon="@mipmap/ic_launcher_round"
 -->
    <application
        android:allowBackup="true"
        android:icon="@drawable/chatsocketsico"
        android:label="@string/app_name"
        android:roundIcon="@drawable/chatsocketsico"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ClaseCliente" />
        <activity android:name=".ClaseServidor" />
        <activity android:name=".ClasePrincipal">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ClaseAbout" />
        <activity android:name=".ClaseChat"></activity>
    </application>

</manifest>